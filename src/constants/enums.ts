export enum EAdUnitName {
  chapterExit = 'chapterExit',
  chapterList = 'chapterList',
  pageList = 'pageList',
}

export enum EFontName {
  montserratAlternatives = 'MontserratAlternates',
}

export enum EFontSize {
  Normal = '14px',
}

export enum EFontWeight {
  Bold = 'Bold',
  Regular = 'Regular',
}

export enum ERemoteConfigKey {
  data = 'data',
}
